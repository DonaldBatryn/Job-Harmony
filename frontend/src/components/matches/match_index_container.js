import { connect } from "react-redux";
import MatchIndex from './match_index'

const msp = state => {
    return ({

    })
}

const mdp = dispatch => {
    return ({

    })
}

export default connect(msp, mdp)(MatchIndex)